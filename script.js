// Smooth scroll to About section (if needed for navigation)
document.getElementById("scrollToAbout")?.addEventListener("click", function () {
    document.getElementById("about").scrollIntoView({ behavior: "smooth" });
});
